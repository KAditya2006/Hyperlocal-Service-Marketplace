services:
  - type: web
    name: instantseva
    env: node
    plan: free
    buildCommand: npm ci && npm run ci
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 22.20.0
      - key: NODE_ENV
        value: production
